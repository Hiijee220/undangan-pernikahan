import{getChatGPTUser}from"../../chatgpt-auth";import{env}from"cloudflare:workers";
export async function POST(req:Request){if(!await getChatGPTUser())return Response.json({error:"Login diperlukan"},{status:401});const fd=await req.formData();const file=fd.get("file");if(!(file instanceof File)||!file.type.startsWith("image/"))return Response.json({error:"File gambar tidak valid"},{status:400});if(file.size>8*1024*1024)return Response.json({error:"Maksimal 8 MB"},{status:400});const ext=file.name.split(".").pop()?.replace(/[^a-z0-9]/gi,"")||"jpg";const key=`wedding/${crypto.randomUUID()}.${ext}`;await env.BUCKET.put(key,await file.arrayBuffer(),{httpMetadata:{contentType:file.type}});return Response.json({url:`/api/images/${encodeURIComponent(key)}`})}
